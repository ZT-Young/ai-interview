# 统一行尾为 LF。
#
# 为什么需要：仓库在 Windows 上开发，而 CI（GitHub Actions）与
# Vercel 构建都在 Linux。Git 默认的 autocrlf 会把检出内容转成 CRLF，
# 于是每次改文件都可能产生整文件级 diff，Linux 侧的 shell 脚本
# 也可能因 CRLF 报 `bad interpreter`。
#
# 显式声明后：仓库内一律存 LF，检出时也不做转换，跨平台 diff 保持干净。
* text=auto eol=lf

# 二进制文件不做行尾转换（否则会被破坏）
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.pdf binary
*.docx binary
*.xlsx binary
*.zip binary
*.woff binary
*.woff2 binary
*.ttf binary
*.exe binary
*.dll binary

# Windows 批处理脚本必须保留 CRLF，否则 cmd 可能解析异常
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf

# 锁文件不做 diff 展示（体积大且无阅读价值）
pnpm-lock.yaml -diff
