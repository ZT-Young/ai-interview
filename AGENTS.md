# AGENTS.md

本文件是本仓库的 **Agent 执行规约**：面向所有在本项目中工作的 AI 编码 Agent（以及人类协作者）。
它规定「必须做什么、禁止做什么、什么算做完」。冲突时以本文件为准。

---

## 0. 一句话契约

> 本仓库是面向 C 端的 AI 模拟面试 Web 应用：求职者粘贴目标岗位 JD、上传简历，AI 扮演面试官完成模拟面试，最后产出评分报告与提升建议。

**每一轮任务必须走完这个循环，不允许跳步：**

```
澄清模糊需求 → 输出计划（含影响文件清单）→ 小步编码 → lint / typecheck / test / build 全绿
→ 自检 DoD 六条 → 给出验收步骤 → 小步提交
```

- 需求模糊 **先提问，不猜测**（见 §9.1）。
- 每步都要可运行；不允许留下「编译不过的中间态」提交。
- 只改与任务相关的文件，**不修改无关文件**。

---

## 1. 产品目标

C 端求职者粘贴目标岗位 JD、上传个人简历，AI 扮演面试官进行模拟面试，最后生成评分报告和提升建议。

**用户价值闭环**：投递前知道「我这个岗位会怎么被问、我答得怎么样、差在哪、怎么补」。

---

## 2. V1 核心流程

### 面试前

1. 用户登录
2. 粘贴或上传 JD
3. 上传简历（PDF / Word / 图片）
4. AI 解析 JD 和简历，**展示解析结果，允许用户修改**
5. 生成面试计划：自我介绍、项目深挖、专业题、行为题、反问

### 面试中

6. 开始面试：AI **一次只问一个问题**，用户文字回答或语音转文字
7. AI 根据回答追问，**最多追问 2 层**

### 面试后

8. 结束面试，生成报告
9. 用户查看报告，付费解锁详细内容
10. 历史记录，可再次训练

> 第 4 步的「允许用户修改」是**产品硬要求**，不是可选优化：解析结果必须可编辑后再进入出题，避免错误解析污染整场面试。
> 第 7 步的「2 层」是**每道主问题**的追问深度上限，需在数据模型上可追踪（见 §6 `Question.depth`）。

---

## 3. 范围边界

### 3.1 V1 范围内（In Scope）

- Web PC + H5（响应式，移动端可用）
- 文字面试为主，支持语音输入转文字（ASR）
- AI 出题、追问、评分、报告
- 用户系统、免费次数、支付解锁
- 管理后台最小版：查看用户、面试、报告、订单

### 3.2 V1 明确不做（Out of Scope）

以下内容 **禁止在 V1 主动实现**。若任务需求命中其一，视为需求变更，**必须先提问确认**，不得自行扩围：

- ❌ 实时视频面试
- ❌ 表情识别、声纹、性格推断、诚信判断
- ❌ 自动招聘决策
- ❌ 多人面试
- ❌ 复杂防作弊
- ❌ 小程序

---

## 4. 技术栈

| 层 | 选型 | 落点约定 |
|---|---|---|
| 前端 | Next.js 14 App Router + TypeScript + Tailwind CSS + shadcn/ui | `app/`、`components/` |
| 后端 | Next.js Route Handlers / Server Actions | 复杂 AI 逻辑**必须**放 `lib/ai` |
| 数据库 | PostgreSQL + Drizzle ORM | `db/`（schema、migration） |
| 鉴权 | **自建**：邮箱 + 密码（scrypt）+ 数据库会话 + HttpOnly Cookie | `lib/auth/`；§9.2 的 Auth.js 选项已弃用 |
| 文件存储 | S3 兼容对象存储（Cloudflare R2 / AWS S3 / MinIO） | 端口在 `lib/storage/`；未配 S3 时**仅开发环境**回退本地文件系统，生产环境直接 503 |
| LLM | OpenAI 兼容接口，支持 DeepSeek / Qwen / GPT | `lib/ai`，模型名走环境变量 |
| ASR | Whisper API 或国内 ASR（选型 TBD） | `lib/asr/`，与 LLM 同层抽象；未配置时返回 503，**不伪造转写文字** |
| 部署 | Vercel + Neon/Supabase + R2/S3 | — |
| 测试 | Vitest（单元/集成）+ Playwright（E2E） | `*.test.ts`、`e2e/` |
| 包管理 | **pnpm** | 不使用 npm / yarn |

**硬性约定**

- 复杂 AI 逻辑（编排、prompt、评分、报告）**只允许**放 `lib/ai`，禁止散落在组件或 Route Handler 内。
- 模型供应商可切换：**不允许**把模型名、endpoint、key 硬编码在业务代码里。
- ⚠️ 上表命令与目录为**待骨架初始化后固化的基线**；骨架落地时须将实际命令回填到 §9.3，避免文档与仓库不一致。

---

## 5. 数据模型

九个核心实体。字段可扩展，但**语义不可混淆**：

| 实体 | 职责 | 关键字段提示 |
|---|---|---|
| `User` | 用户 | 账号、会员状态、剩余免费次数 |
| `Resume` | 简历文件、解析文本、结构化信息 | 原件存储 key、原始文本、结构化 JSON |
| `JobJD` | 岗位 JD、解析要求 | 原文、结构化要求 |
| `InterviewSession` | 面试会话、状态、配置 | 状态机、面试配置（时长/题目数/难度） |
| `Question` | 问题、类型、来源、顺序 | 类型、来源（JD/简历/通用）、序号、**追问层级 `depth`** |
| `Answer` | 用户回答、转写文本、耗时 | 文本、ASR 转写、**耗时**、是否语音 |
| `Evaluation` | 逐题评分、反馈 | 维度分、反馈、**证据引用** |
| `Report` | 总报告、维度分、建议 | 总分、六维分、亮点/问题/参考回答/建议 |
| `Payment` | 订单、会员、次数 | 订单状态、**会员与次数分离**（订阅制与按次付费并存） |

**建模要求**

- `Question` ↔ `Answer` ↔ `Evaluation` 三者按题一一可追溯，报告任一分数必须能回溯到具体回答。
- 追问关系的父子结构必须可表达（父问题 → 追问 1 → 追问 2），便于计算「追问层级」上限。
- 会话状态必须是**显式状态机**，非法状态迁移要在服务层拒绝。

---

## 6. AI 面试规则

### 6.1 硬约束（违反即为 Bug）

- **N1** 问题必须基于 JD 和简历，不脱离用户实际背景泛泛而问。
- **N2** 一次只问一个问题。
- **N3** 追问围绕 STAR：背景、任务、行动、结果、数据。
- **N4** **不编造用户经历**，不替用户虚构项目、数字或头衔。
- **N5** **不问敏感问题**：年龄、性别、婚育、宗教、政治等。
- **N6** 评分必须**引用回答证据**，禁止无证据给分。
- **N7** 报告必须区分四类内容：**亮点、问题、参考回答、下一步建议**。

### 6.2 评分规则

- 维度（六项）：**岗位匹配、专业能力、项目深度、逻辑表达、沟通表达、动机稳定性**
- 每项 **0–5 分**；总分 **0–100**
- 维度分 → 总分的换算公式必须**显式实现且可单测**，不得散落在模板里算。

### 6.3 实现要求

- 所有 prompt 与规则集中在 `lib/ai`，**规则不得散落在页面组件中**。
- N1–N7 每一条都应能在代码中找到明确落点；新增/修改 AI 行为时同步更新本节与对应测试。
- LLM 输出必须做**结构化校验**（schema 校验失败要重试或降级），不信任自由文本直出。

---

## 7. 合规红线

- **C1 知情同意**：用户必须知情同意后才能开始面试（含 AI 生成内容说明）。
- **C2 法律文本**：隐私政策、用户协议必须可访问。
- **C3 数据安全与删除**：数据加密存储，**用户可删除**个人数据。
- **C4 内容标识**：AI 生成内容必须明确标识。
- **C5 决策边界**：**不得仅凭 AI 自动拒绝候选人**——AI 结果只作参考与提升建议。
- **C6 审计日志**：保留审计日志。

**额外要求**：发送给 LLM 的简历/JD 需做必要的 PII 处理与最小化；删除用户数据须覆盖数据库与对象存储。

---

## 8. 开发规范

- 先计划，后编码。
- 小步提交，每步可运行。
- **不硬编码密钥**，使用 `.env.example` 声明变量；真实密钥只进本地 `.env*`（且不入库）。
- 每完成一个模块，运行 **lint、typecheck、test、build**。
- 修改前**说明影响文件**。
- **不修改无关文件**（禁止顺手重构、格式化无关代码）。
- 遇到模糊需求**先提问**，不猜测。

---

## 9. 工作流细则

### 9.1 何时必须停下来提问

任一条成立，先提问再动手：

- 需求命中 §3.2 非目标清单
- 涉及鉴权方案、支付渠道、模型供应商的**选型未定**
- 需要改动数据模型已定字段的语义
- 需要跨模块重构 / 触及 §8「不修改无关文件」边界
- 验收标准无法客观判定

### 9.2 待澄清事项（未定，禁止自行选型）

- [x] 鉴权：**已定为自建**（邮箱+密码 scrypt + 数据库会话 + HttpOnly Cookie），见 §4
- [ ] 支付渠道与解锁模型（单次解锁 / 会员 / 次数包）的具体组合
- [ ] ASR 供应商（Whisper 或国内 ASR）与降级策略
- [ ] 免费次数规则（新用户额度、是否每日重置）

### 9.3 完成定义（DoD）

一个模块/任务只有同时满足以下六条才算完成，缺一不可：

1. **代码可运行** —— 且**必须对新增/修改的路由发真实 HTTP 请求验证**（`curl` / `Invoke-WebRequest`）。
   `pnpm test` 通过**不等于**路由可用：依赖打包问题（如 pdfjs 被 webpack 打包后加载即抛错）
   只在真实请求时暴露，单元测试完全看不到。
2. **有错误处理**（含外部依赖失败：LLM、ASR、存储、支付）
3. **有基本测试**
4. **通过 lint / typecheck / build**
5. **更新相关文档**（含本文件受影响章节）
6. **给出验收步骤**（可复现的操作路径，而非「已实现」的口头结论）

汇报格式固定为：**改动文件 → 做了什么 → 验证结果 → 验收步骤 → 遗留问题**。

### 9.4 验证命令（已随骨架固化）

```bash
pnpm lint        # ESLint
pnpm typecheck   # tsc --noEmit
pnpm test        # Vitest（单元 + 集成；集成缺少 DATABASE_URL 时显式跳过）
pnpm build       # Next.js 生产构建
pnpm e2e         # Playwright（需先启动 pnpm dev）
pnpm db:migrate  # Drizzle 迁移
```

**集成测试约定**：缺少 `DATABASE_URL` / `AUTH_SECRET` 时**显式跳过**，
不得用假断言伪装通过；交付时必须说明跳过了多少条。

### 9.5 编辑文档的注意事项

- 修改 Markdown 时**不要用 shell 的字符串替换**（PowerShell 中反引号是转义字符，
  容易写出破坏性替换且无版本控制可回滚）。用编辑工具做精确替换，或整体重写。
- 仓库已初始化 git（分支 `main`）。改动大文件前仍须确认内容来源；
  提交粒度见 §8「小步提交」。**切勿用 `git add -A` 盲提交** —— 工作区可能存在
  未忽略的临时文件与个人材料（历史踩坑记录见 `.gitignore` 末尾注释）。

---

## 10. 完成定义（DoD）→ 见 §9.3

完成定义统一由 §9.3 规定，本节不再重复，避免两处漂移。
汇报格式固定为：**改动文件 → 做了什么 → 验证结果 → 验收步骤 → 遗留问题**。

